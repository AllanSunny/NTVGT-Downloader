const youtubedl = require("youtube-dl");
const ffmpeg = require("js-ffmpeg");
const fs = require("fs");


//Start and end times are in hh:mm:ss format
function downloadSong(ytLink, startTime, duration, destination) {
    console.log("Heblo")
}


module.exports = {
    downloadSong,
};